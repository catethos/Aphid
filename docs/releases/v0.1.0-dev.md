# Aphid v0.1.0-dev — native validation draft

Private review only. No target is release-supported; no stage is complete.
This package is not published to Hex. Attribution, minimum-system execution
and actual public delivery remain open. Creation is not authorized by these notes.

| Native archive | SHA256 |
|---|---|
| `aphid-0.1.0-dev-x86_64-linux-gnu.tar.gz` | `fa9535c7e8f3b149658ed0f5f98329f7142cfd21f30f20fef765c74c42b798cc` |
| `aphid-0.1.0-dev-aarch64-linux-gnu.tar.gz` | `235ceb0490a1baf64e33fd53f17f683ce263197cf0cac6cf1107275dd94b62af` |
| `runtime-validation-aarch64-macos-13.3-baseline-3.tar.gz` | `3125907a738162c55ba8f68874c861c767bc7a5f17e9b23c69f028df192e3034` |

Linux native provenance remains qualification run
https://github.com/catethos/Aphid/actions/runs/34193449987.
The unchanged archives passed refreshed consumers in
https://github.com/catethos/Aphid/actions/runs/34205212486.
One exact source package, SHA256
`11ca5ca447a7a93ee6d8e2d79758f3ed67d9ce04d20947a08e82598851152507`,
passed all 92 tests on both native Linux architectures and macOS ARM64 with
fresh caches, normally acquired locked dependencies, denied native compilers
and development-tree reads, and unchanged native hashes. Linux bundled-ERTS
validation releases additionally passed 92 tests, embedded startup/restart,
offline shutdown/persistent reopen and missing/corrupt/unloadable startup checks.
Those ephemeral release archives are not part of this draft.

The source package installs `THIRD_PARTY_NOTICES.txt` alongside the native
archive's license tree; the receipt checks all native and license hashes on
repeat installation. The supplement SHA256 is
`08cc1d20ccb4808537545220152d6aff97701e2c3b0b8597ab2dadaa82a789d1`.
It preserves ten retained texts, without inventing Pegasus/ZigParser's missing
version-appropriate grants or claiming final generated/linked-component,
modified-fork, NimbleParsec or production-runtime attribution clearance.

The proposed complete draft has thirteen assets, recorded in
`docs/releases/v0.1.0-dev-assets.json`. The Linux workflow creates nine
Linux/notice assets and `SHA256SUMS` covers its eight payloads. Four local macOS
review assets are attached in a separate step under the same explicit scope;
`SHA256SUMS-macos` covers its three payloads. The draft must stay private while
that attachment is incomplete. Both checksum groups and all thirteen local
asset hashes have been checked. No bundled ERTS, build caches or upstream
extension archives are proposed for upload.

Execution is proved on Ubuntu 24.04/glibc 2.39 and macOS 26.6 only, with Elixir
1.20.0 and OTP 29.0.4. Linux installation needs GNU objcopy; macOS needs otool.
The Linux engine requires GLIBC 2.38 and GLIBCXX 3.4.32. macOS native commands
declare 13.3. Neither establishes a tested minimum OS/CPU. Other OTP/binutils,
the required x86_64-to-ARM64 cross-build, source installation and outstanding
plan gates remain open. DuckDB remains 1.4.4.

Defaults remain disabled. A private draft is not an anonymous public endpoint;
public publication requires separate explicit authorization, followed by final
source-package proof against the actual repository URLs on all three targets.
Hex publication and external maintainer messages remain excluded.
