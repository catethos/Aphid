# Public publication decision for existing draft 384589981

2026-09-08. Recommendation: **hold public publication** pending the attribution
review below. This is a reviewable decision record, not publication authorization
or a determination of redistribution rights. No target is release-supported;
no implementation or stage is complete. Hex publication remains excluded.

## Exact proposed operation and content

If separately authorized after review, publish the **existing** draft release ID
`384589981` as a prerelease (`draft=false`, `prerelease=true`). Keep the signed tag
`v0.1.0-dev` fixed at `750dc79ec3607893fe53ba84c2cfe45158c49bc3`. Do not create
another release, move the tag, replace any of the thirteen assets, or include
bundled ERTS. The proposed body replacement is retained locally in
[v0.1.0-dev-public-body.md](v0.1.0-dev-public-body.md); it has not been sent to GitHub. The exact names, byte counts and hashes are in the historical
[v0.1.0-dev asset matrix](v0.1.0-dev-assets.json). Its preauthorization wording
is historical; [draft evidence](../evidence/draft-release-1.md) establishes creation.

The fresh [release-ID read](../evidence/publication-draft-state-2.json) still
records `draft=true`, `prerelease=true`, `published_at=null` and thirteen matching
names/sizes/GitHub digests. The earlier authenticated streaming download evidence
remains [download checks](../evidence/draft-release-download-checks-1.json);
this review did not repeat those downloads. The initial release-by-tag 404 is
preserved; use the release ID for review, never treat a draft tag lookup failure
as permission to create a duplicate.

The qualified source package is **not one of the thirteen release assets**:
`artifacts/notices-source-linux-2/aphid-current-consumer/aphid-0.1.0-dev-combined.tar`,
SHA256 `11ca5ca447a7a93ee6d8e2d79758f3ed67d9ce04d20947a08e82598851152507`.
GitHub's automatic source snapshots describe the fixed tag, not that Hex archive
or any future URL-enabled final package. Publication of the existing draft does
not publish an Aphid package to Hex.

## Attribution decisions still required

| Content / scope | Retained evidence | What closes the gap |
|---|---|---|
| Pegasus 0.2.6, compile-time dependency; possible generated-code relevance | All 14 package files match `6f955046465d97fcef3ade6da47e7ed80e6ca3c8`; metadata declares MIT; full historical notice missing | Authoritative full copyright/permission notice applicable to that version, plus documented generated/shipped-content applicability. Later Pegasus text is not substituted. |
| ZigParser 0.7.0, compile-time dependency; possible generated-code relevance | All 45 files match `3ab4aa2ff10d1886a33a6ea0fc1c908712496360`; no full notice in inspected history | Authoritative version-specific notice and applicability review. ZigGet's grant is not borrowed. |
| NimbleParsec 1.4.2 | Complete pinned README, including its copyright and Apache-2.0 notice, is in the installed supplement | Determine full license-text and generated-content coverage for the final shipped set; a README license reference alone does not establish completed coverage. |
| Linked/header-derived native and generated components | 57 candidate component groups, 41 direct link inputs, retained header records; seven embedded preambles shipped in supplement | Final component-to-notice mapping, including generated code and retained object/header contents. Current macOS records are not a final linker map or Linux object-retention proof. Capture stronger provenance at the next necessary isolated source build; do not rebuild unchanged candidates solely to repeat this inventory. |
| Modified Ladybug httplib | Pinned fork matches engine source; full upstream 0.14.2 text is associated and shipped; full diff retained | Review modified-fork attribution against the recorded substantive differences. Do not describe the diff as formatting-only. |
| Production ERTS/static/build contents | 22 OTP source notices, version-matched OTP OpenSSL 3.5.7 notice, 303 matching installed sources and six generated-source exceptions | Exact production runtime/build/static inventory and notice delivery, including generated-source provenance. ERTS is excluded from these thirteen assets, so this remains a production Mix-release gate rather than a claim that the draft bundles ERTS. Native static provenance remains relevant to the draft. |

Evidence: [attribution follow-up](../evidence/attribution-followup.md),
[notice sources](../evidence/notice-sources.md),
[native map](../evidence/native-notice-map.md),
[installed supplement](../evidence/shipped-notices-1.md), and
[content recheck](../evidence/publication-content-review-2.json).

The archives retain 73 license members per Linux target and 67 for macOS. The
source supplement retains ten texts and installs alongside those archive notices.
Counts and whole-file hashes are inventory facts, not proof that all necessary
attribution is covered. No notice is deleted based on presumed non-use.
Build-only dependency classification does not by itself resolve generated output.
No exhausted Pegasus/ZigParser history search or maintainer message was repeated.
The next attribution input must come from an authoritative version-specific
source supplied for review; contacting maintainers needs separate authorization.

## Publication and installation sequence

1. Resolve/document the attribution decisions above and review the [exact proposed public body](v0.1.0-dev-public-body.md). Existing notes contain historical private/proposal wording;
   changing the remote body or asset contents is outside the exact unchanged
   publication operation and must be included explicitly in the approval scope.
2. Obtain separate explicit authorization naming release ID `384589981`, its
   fixed tag, thirteen-asset matrix, public prerelease status and any exact body
   revision. No authorization is requested before these decisions are ready.
3. After authorized publication, stream-check anonymous HTTPS delivery for all
   thirteen assets against the approved independent pins. Authenticated private
   downloads do not establish this. Never re-pin to whatever a server returns.
4. Enable only the three existing catalog `url` fields at
   `https://github.com/catethos/Aphid/releases/download/v0.1.0-dev/` plus the exact
   corresponding archive filename. Preserve every native identity/hash and tag.
   Make a new signed source commit; the old tag/package does not gain those edits.
5. Build and qualify one exact final source package. The existing manual
   `linux-consumer.yml` first proves its explicit local-archive consumers and
   embedded releases, with approved seven-day passing source retention. Record
   the identical source SHA256 from both native jobs. Retrieve before expiry.
6. Dispatch the prepared `public-consumer.yml` on the reviewed source revision,
   with that successful `source_run` and independent `source_sha256`. Both native
   Linux jobs download the exact same retained source bytes, obtain an anonymous
   native archive as the independent oracle, and invoke `--package-default`.
   The real Mix adapter separately downloads using the package catalog, with no
   APHID installation overrides. Public HTTPS is allowed during compilation;
   compiler/development-read denial remains, and runtime reverts to offline.
   This workflow has read-only permissions and no uploads or publication step.
7. Run the same exact package/default harness on native macOS ARM64, using the
   retained macOS archive as oracle and new workspace/log names. Check all 92
   tests, native and license hashes, absent compiler invocations and empty caches.
   Retain the final source pin and all three input records/logs. A new macOS
   bundled-ERTS proof remains separate; older proof is scoped to its old source.

Example for step 7, only after the final package has enabled reviewed URLs:

```sh
python3 -u scripts/precompiled_consumer.py \
  --archive artifacts/runtime-validation-aarch64-macos-13.3-baseline-3.tar.gz \
  --sha256 3125907a738162c55ba8f68874c861c767bc7a5f17e9b23c69f028df192e3034 \
  --package /absolute/path/to/final-source.tar \
  --package-sha256 "$REVIEWED_FINAL_SOURCE_SHA256" \
  --destination /private/tmp/aphid-public-macos-NEW \
  --hex-dependencies --package-default
```

Current catalogs remain disabled. The prepared workflow has not run; no public
endpoint or new consumer/runtime pass is claimed. Retained Actions qualification
copies expire around September 15; local reviewed native sets remain preserved.
The oracle is never injected as an installer override in package-default mode.

Linux execution is proved only on Ubuntu 24.04/glibc 2.39; the engine requires
GLIBC 2.38 and GLIBCXX 3.4.32, and installation requires GNU objcopy. macOS
execution is only on 26.6; native 13.3 declarations do not prove the minimum,
and installation requires otool. Minimum-system/CPU, other OTP/binutils,
x86_64-to-ARM64 cross-build, fresh source installation, production provenance,
and Sections 8–9 / Stage 05/07/08/09 gates remain open. DuckDB remains 1.4.4.
