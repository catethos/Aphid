# Expanded experimental Hex authorization

2026-09-08. Starting source: `494310c891cba2873e3dd5b32f4e521874ba1acf`.
Working directory: `/Users/catethos/workspace/ladybugex/zig_library`.

After discussing publication before testing additional Linux systems, the owner
said: "ok i want to include Hex now, change the scope". This authorizes the
experimental `aphid` `0.1.0-dev` Hex publication sequence: prepare the source
package with the Zig notice and reviewed public catalog URLs, check the final
package locally on macOS ARM64, publish to Hex, and leave Linux package and
additional-system qualification as post-publication work. No further publication
approval is needed for that sequence; an authenticated publishing account is
still technically required.

The fixed GitHub tag and all thirteen public native assets remain unchanged.
No native rebuild, maintainer message, additional GitHub release, stable/support
claim or implementation-stage completion is authorized by this scope change.
Package-only publication is selected because no HexDocs generator is configured.
The new source package is not the fixed GitHub tag snapshot.

Earlier no-Hex instructions and the before-publication three-target gate are
superseded for this experimental package. The existing evidence remains historical
and intact. Failures found during preparation must still be addressed or reported.
